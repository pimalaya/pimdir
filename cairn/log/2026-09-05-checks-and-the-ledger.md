---
cairn: log
change: checks-and-the-ledger
date: 2026-09-05
---

# A rule lands with the check that fails without it

Every audit of the format found more, and every one found what the last had already written down. Reading prose is an unbounded oracle; what converges is a check. Two join checks/: invariants.sh runs the canonical statements, bound through the sqlite3 CLI and read from queries/ verbatim, through the scenarios the parts argue from (the feed's cursor and stamps, a rename, the trash, the move exception, the producer's pin against the collector, the drain order, a rename into a pending move, a collection's removal) and asserts what each promises; names.sh keeps the documents and queries/ naming the same statements in both directions, which found five statements no document named. `nix flake check` runs both beside the schema and the vectors.

Nine sync vectors join the twenty-four: a vanished local edit, flag removals, the union over an unknown base, a two-source divergence, a rekey with a pending create, a `Full` fetch moving the base, a fallback key minting, a create waiting for a probe, a remove settling a conflict. checks/vectors.py checks the `alt:` key, the provisional handle form and the absence of the delete option.

AGENTS.md carries the rule from here: a change to a rule, a statement or the schema lands with the scenario, vector or case that fails without it; a log entry names the rules it touches and their guards; audits are differential, scoped to the entries since the last, and start from the ledger under cairn/changes/, where `algorithm-audit-2026-09-04` records every finding of the audit that led here and what closed it.
