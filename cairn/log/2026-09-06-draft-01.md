---
cairn: log
change: draft-01
date: 2026-09-06
---

# The text is pinned as draft-01

The standard's first pinned text. STORAGE, SYNC and SEARCH carry `Status: draft-01`, the README says what a draft number is (a name for a text, bumped in the three Status lines and the README when a normative part changes after the tag, never a store version), and AGENTS §2 says the tag is the user's. io-pimdir states that it implements draft-01 and that spec/ is that text's SQL byte for byte. Schema versions stay 1: a draft names what an implementation was checked against, the migrations still say what a store is.
