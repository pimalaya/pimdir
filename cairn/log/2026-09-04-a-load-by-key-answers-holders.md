---
cairn: log
change: a-load-by-key-answers-holders
date: 2026-09-04
---

# A load by link id answers with the rows the source binds

The projection offers a `Created` placement for every item the source lacks and the store holds a body for, so the merge derives the append. Read by key while an upgrade settled a fetched identity, the offer passed for a holding of this source, and the second source of a shared item minted `dup:<hint>#<handle>` for the resource it had always held, where §6 mints only when this source binds the hint under another handle. Found by neverest's two-endpoint suites over the merged io-pimdir.

SYNC §10 and GUIDE §9 now say a `Links` load returns the placements the source binds and never the offer.
